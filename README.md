# manga.chrstnfrrs.com
### Running the project

Install dependencies and run the project in development mode:

```bash
yarn install
yarn dev
```

This will start the development server on [localhost:3000](http://localhost:3000).
